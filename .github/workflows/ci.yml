name: CI

on:
  push:
  pull_request:

jobs:
  check:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dsh (needed by the smoke test's version fallbacks)
        timeout-minutes: 5
        run: npm install -g @deepseek-ai/dsh --no-audit --no-fund

      - name: Syntax check
        shell: bash
        run: |
          for f in lib/*.js scripts/*.mjs; do
            node --check "$f"
          done

      - name: Smoke test (real detection cycle, autoApply forced off)
        timeout-minutes: 3
        run: node scripts/dev-smoke.mjs

      - name: UI smoke test (client module + channel handler, offline)
        timeout-minutes: 1
        run: node scripts/ui-smoke.mjs

      - name: Offline update and rollback regression tests
        timeout-minutes: 2
        run: node --test tests/client.test.mjs tests/regression.test.mjs
